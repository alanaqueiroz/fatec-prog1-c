// 3. Uma loja fornece 10% de desconto para funcionários e 5% de desconto para clientes vips. 
// Faça um programa que calcule o valor total a ser pago por uma pessoa. 
// O programa deverá ler o valor total da compra efetuada e um código que identifique se o comprador 
// é um cliente comum (1), funcionário (2) ou vip (3).

#include <stdio.h>

int main()
{
    string cliente;

    printf("Que tipo de cliente voce, digite 'f' para funcionario e v para (vip): ");
    scanf("%f", &cliente);
    
    if(cliente = f)
    {
        cliente = m * 3.30;
        printf("Os valor das macas saira por %d", vf);
    }
    
    if (m >= 12)
    {
        vf = m * 3.00;
        printf("O valor das macas saira por %d", vf); 
    }

    return 0;
}
